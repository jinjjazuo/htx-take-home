# Optional — only needed to reproduce the parser bake-off in part1.ipynb §1.2-1.3
# (docling vs markitdown vs pymupdf vs pdfplumber). NOT required to run the extraction
# pipeline itself (§2 onward), which only needs pdfplumber - see requirements.txt.
#
# Heavy: pulls in torch + transformers (~1.8GB installed) and downloads model weights
# from Hugging Face Hub the first time docling runs. Needs internet access.
#
#   pip install -r requirements.txt -r requirements-parser-eval.txt

docling==2.126.0
markitdown==0.1.7

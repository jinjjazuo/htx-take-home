# Part 1 — Document Extraction & Prompt Engineering
# Python 3.14. Install into a venv:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt

# PDF parsing
pdfplumber==0.11.10          # primary parser - keeps table rows on one line (see notebook §1.5)
pymupdf==1.28.2              # fast text extraction, used for the page-number check in §1.1

# Optional: docling + markitdown are compared in notebook §1.2-1.3 but are NOT required
# to run this pipeline - see requirements-parser-eval.txt (heavy: ~1.8GB, needs internet).

# LLM orchestration
langchain==1.4.0
langchain-core==1.6.2
langchain-google-genai==4.4.0
pydantic==2.13.5             # extraction schemas / structured output

# Environment + notebook
python-dotenv==1.2.3
ipykernel==7.3.0
